# Faça uma função calculadora de dois números com três parâmetros
# Autor: Julio Cesar Ferreira  
# # 
# # Arquivo: Faça uma função calculadora de dois números com três parâmetros: os dois primeiros serão os números da operação e o terceiro será a entrada que definirá a operação a ser executada. Considere a seguinte definição:
1. Soma
2. Subtração
3. Multiplicação
4. Divisão

Caso seja inserido um número de operação que não existe, o resultado deverá ser 0.
##############################################################################

a = float(input("Primeiro número:"))
b = float(input("Segundo número:"))
operação = input("Digite a operação a realizar (+,-,* ou /):")
if operação == "+":
    resultado = a + b
elif operação == "-":
    resultado = a - b
elif operação == "*":
    resultado = a * b
elif operação == "/":
    resultado = a / b
else:
    print("Operação inválida!")
    resultado = 0
print("Resultado: ", resultado)






Outra Forma de fazer:
def calculadora(num1, num2, operacao):
    if operacao == 1:  # Soma
        return num1 + num2
    elif operacao == 2:  # Subtração
        return num1 - num2
    elif operacao == 3:  # Multiplicação
        return num1 * num2
    elif operacao == 4:  # Divisão
        if num2 != 0:  # Verifica se o divisor não é zero
            return num1 / num2
        else:
            return "Erro: Divisão por zero"
    else:  # Operação não reconhecida
        return 0

# Exemplo de uso:
resultado = calculadora(10, 5, 1)  # Soma: 10 + 5
print(resultado)  # Saída: 15

C
Parâmetros : num1enum2são os números comooperacaodetermina qual o
Operações : A foperacaopara decidir qual cá
Divisão : H
Retorno : Se aoperacaonão
